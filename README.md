Build commands in makefile

Development requires BunJS/NodeJS

To use theme, open theme.css and copy the whole thing to your clipboard,
then go to Fluxer: User Settings -> Look & Feel -> Theme -> Custom Theme Tokens -> scroll down... -> Custom CSS Overrides and paste.
